---
const principles = [
  {
    icon: 'learn',
    title: 'LEARN',
    description: 'Build Knowledge',
    width: 63,
    height: 64,
  },
  {
    icon: 'ship',
    title: 'SHIP',
    description: 'Release',
    width: 59,
    height: 60,
  },
  {
    icon: 'experiment',
    title: 'EXPERIMENT',
    description: 'Test Ideas',
    width: 63,
    height: 64,
  },
  {
    icon: 'impact',
    title: 'IMPACT',
    description: 'Change',
    width: 63,
    height: 64,
  },
  {
    icon: 'research',
    title: 'RESEARCH / BUILD',
    description: 'Discover / Create',
    width: 59,
    height: 60,
  },
];
---

<section
  id="our-philosophy"
  class="philosophy"
  aria-labelledby="philosophy-title"
>
  <div class="canvas">
    <div class="illustration" aria-hidden="true">
      <picture>
        <source
          srcset="/images/philosophy/sorcerer-1x.webp 861w, /images/philosophy/sorcerer-2x.webp 1722w"
          sizes="(max-width: 760px) 100vw, 861px"
          type="image/webp"
        />
        <img
          src="/images/philosophy/sorcerer-1x.webp"
          width="861"
          height="770"
          alt=""
          loading="lazy"
          decoding="async"
        />
      </picture>
      <span class="fx">
        <span class="aura"></span>
        <span class="pulse"></span>
        <span class="spark spark-a"></span>
        <span class="spark spark-b"></span>
        <span class="spark spark-c"></span>
      </span>
    </div>
    <div class="content">
      <div class="heading">
        <span class="eyebrow">Our Philosphy</span>
        <h2 id="philosophy-title">
          <span>We Don't</span>
          <span>Just Learn AI</span>
          <span>We Build With It</span>
        </h2>
      </div>
      <ul class="principles" aria-label="Our principles">
        {principles.map(({ icon, title, description, width, height }) => (
          <li>
            <img
              src={`/images/philosophy/${icon}.webp`}
              width={width}
              height={height}
              alt=""
              loading="lazy"
              decoding="async"
            />
            <div class="principle-copy">
              <h3>{title}</h3>
              <p>{description}</p>
            </div>
          </li>
        ))}
      </ul>
    </div>
  </div>
</section>

<style>
  .philosophy {
    position: relative;
    container-type: inline-size;
    isolation: isolate;
    overflow: clip;
    background: var(--color-background);
  }
  .canvas {
    position: relative;
    min-height: 837px;
    max-width: 1440px;
    margin-inline: auto;
  }
  /* The exported blur extends 350px beyond each side of the 280px ellipse. */
  .canvas::before {
    content: '';
    position: absolute;
    left: 845px;
    top: 298px;
    width: 980px;
    height: 980px;
    background: url('/images/philosophy/glow.webp') center / contain no-repeat;
    pointer-events: none;
  }
  .illustration {
    position: absolute;
    left: 0;
    top: 12px;
    width: 861px;
    height: 770px;
    pointer-events: none;
  }
  .illustration picture {
    display: block;
    width: 100%;
    height: 100%;
  }
  .illustration img {
    display: block;
    width: 100%;
    height: 100%;
  }
  /* Crystal-centered FX. Hidden unless motion is allowed so the reduced-motion
     render stays pixel-identical to the reference export. */
  .fx {
    --crystal-x: 57%;
    --crystal-y: 52%;
    --u: 1;
    position: absolute;
    inset: 0;
    opacity: 0;
    pointer-events: none;
  }
  .fx .aura,
  .fx .pulse,
  .fx .spark {
    position: absolute;
    left: var(--crystal-x);
    top: var(--crystal-y);
    mix-blend-mode: screen;
  }
  .aura {
    width: 62%;
    aspect-ratio: 1;
    translate: -50% -50%;
    background: conic-gradient(
      from 0deg,
      transparent 0deg,
      rgb(155 123 255 / 50%) 45deg,
      transparent 110deg,
      transparent 190deg,
      rgb(108 59 255 / 42%) 235deg,
      transparent 300deg
    );
    -webkit-mask: radial-gradient(
      circle closest-side,
      transparent 32%,
      #000 54%,
      transparent 92%
    );
    mask: radial-gradient(
      circle closest-side,
      transparent 32%,
      #000 54%,
      transparent 92%
    );
  }
  .pulse {
    width: 42%;
    aspect-ratio: 1;
    translate: -50% -50%;
    background: radial-gradient(
      circle closest-side,
      rgb(160 130 255 / 55%),
      transparent 82%
    );
  }
  .spark {
    width: var(--s);
    height: var(--s);
    margin: calc(var(--s) / -2) 0 0 calc(var(--s) / -2);
    border-radius: 50%;
    background: #d8ccff;
    box-shadow: 0 0 10px 2px rgb(139 92 246 / 75%);
  }
  .spark-a {
    --s: 5px;
    --r: 150px;
    --a: 0deg;
  }
  .spark-b {
    --s: 6px;
    --r: 205px;
    --a: 140deg;
  }
  .spark-c {
    --s: 4px;
    --r: 118px;
    --a: 250deg;
  }
  @media (min-width: 1441px) {
    .illustration {
      /* Anchor the artwork to the section edge outside the centered canvas. */
      left: calc((1440px - 100cqw) / 2);
    }
  }
  .content {
    position: relative;
    margin-left: 855px;
    padding-top: 150px;
    width: 471px;
    display: flex;
    flex-direction: column;
    gap: 74px;
  }
  .heading {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 14px;
  }
  .eyebrow {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 93px;
    height: 26px;
    padding: 4px 8px;
    border-radius: 32px;
    background: rgb(255 255 255 / 15%);
    font-size: 12px;
    font-weight: 400;
    line-height: 18px;
    white-space: nowrap;
    box-shadow: inset 0 1px 1px rgb(255 255 255 / 40%);
  }
  h2 {
    margin: 0;
    width: 471px;
    font-family: var(--font-heading);
    font-size: 56px;
    font-weight: 400;
    line-height: 68px;
    background: linear-gradient(270deg, #fff, #ede8ff);
    background-clip: text;
    color: transparent;
  }
  h2 span {
    display: block;
    white-space: nowrap;
  }
  .principles {
    margin: 0;
    padding: 0;
    list-style: none;
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 30px 92px;
  }
  li {
    display: flex;
    align-items: center;
    gap: 20px;
    min-width: 0;
  }
  li img {
    display: block;
    flex-shrink: 0;
    object-fit: contain;
  }
  .principle-copy {
    flex-shrink: 0;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  h3,
  p {
    margin: 0;
    white-space: nowrap;
  }
  h3 {
    font-size: 18px;
    font-weight: 700;
    line-height: 27px;
  }
  p {
    font-size: 14px;
    font-weight: 400;
    line-height: 21px;
  }
  .principles li h3 {
    transition: color 0.35s ease;
  }
  @media (prefers-reduced-motion: no-preference) {
    .fx {
      opacity: 1;
    }
    .aura {
      animation: phil-aura 18s linear infinite;
    }
    .pulse {
      animation: phil-pulse 5.5s ease-in-out infinite;
    }
    .spark-a {
      animation: phil-orbit 11s linear infinite;
    }
    .spark-b {
      animation: phil-orbit 16s linear infinite;
    }
    .spark-c {
      animation: phil-orbit 8.5s linear infinite;
    }
    .illustration picture {
      animation: phil-float 7s ease-in-out infinite;
    }
    .principles li {
      transition: transform 0.35s ease;
    }
    .principles li:hover {
      transform: translateY(-3px);
    }
    .principles li img {
      transition:
        transform 0.35s ease,
        filter 0.35s ease;
    }
    .principles li:hover img {
      transform: scale(1.1);
      filter: drop-shadow(0 0 12px rgb(108 59 255 / 85%)) brightness(1.15);
    }
    .principles li:hover h3 {
      color: #c9b8ff;
    }
  }
  @keyframes phil-aura {
    to {
      transform: rotate(360deg);
    }
  }
  @keyframes phil-pulse {
    0%,
    100% {
      opacity: 0.35;
      transform: scale(0.94);
    }
    50% {
      opacity: 0.7;
      transform: scale(1.08);
    }
  }
  @keyframes phil-orbit {
    from {
      transform: rotate(var(--a)) translateX(calc(var(--r) * var(--u)));
    }
    to {
      transform: rotate(calc(var(--a) + 360deg))
        translateX(calc(var(--r) * var(--u)));
    }
  }
  @keyframes phil-float {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }
  @media (min-width: 761px) and (max-width: 1399px) {
    .fx {
      --u: 0.62;
    }
    .canvas {
      min-height: 760px;
    }
    .illustration {
      width: 60%;
      height: auto;
      aspect-ratio: 861 / 770;
      top: 100px;
    }
    .content {
      margin-left: 57%;
      padding-top: 120px;
      width: calc(43% - var(--page-gutter));
      gap: 56px;
    }
    h2 {
      width: 100%;
      font-size: clamp(28px, 3.89vw, 54px);
      line-height: 1.214286;
    }
    .principles {
      column-gap: 24px;
    }
    li {
      gap: 10px;
    }
    li img {
      width: 42px;
      height: 44px;
    }
    h3 {
      font-size: 13px;
      line-height: 21px;
    }
    p {
      font-size: 11px;
      line-height: 18px;
    }
    .canvas::before {
      left: calc(80% - 350px);
    }
  }
  @media (max-width: 760px) {
    .fx {
      --u: 0.42;
    }
    .canvas {
      min-height: 0;
      display: flex;
      flex-direction: column;
      padding: 64px var(--page-gutter) 0;
    }
    .content {
      margin-left: 0;
      padding-top: 0;
      width: 100%;
      gap: 40px;
    }
    h2 {
      width: 100%;
      font-size: clamp(30px, 7.2vw, 50px);
      line-height: 1.214286;
    }
    .principles {
      column-gap: 20px;
      row-gap: 26px;
    }
    li {
      gap: 10px;
    }
    li img {
      width: 42px;
      height: 44px;
    }
    h3 {
      font-size: 13px;
      line-height: 21px;
    }
    p {
      font-size: 11px;
      line-height: 18px;
    }
    .principle-copy {
      gap: 4px;
    }
    .illustration {
      position: relative;
      order: 1;
      left: calc(-1 * var(--page-gutter));
      top: auto;
      width: calc(100% + 2 * var(--page-gutter));
      height: auto;
      aspect-ratio: 861 / 770;
      margin-top: 24px;
    }
    .canvas::before {
      left: calc(65% - 350px);
      top: -90px;
      opacity: 0.5;
    }
  }
  @media (max-width: 360px) {
    .principles {
      grid-template-columns: 1fr;
    }
    li img {
      width: 50px;
      height: 52px;
    }
    h3 {
      font-size: 15px;
    }
    p {
      font-size: 13px;
    }
  }
</style>
